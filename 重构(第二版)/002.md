# 提炼函数(有局部变量)

```text
当存在以下两种局部变量时，可将提炼为带有参数的函数
需要处理“只存在于源函数作用域”的变量，包括源函数的参数以及源函数内部定义的临时变量。

一、有局部变量，但只是读取这些变量的值，并不修改它们。
将 printDetails 提炼为顶层函数，就需要将这些局部变量当作参数传给目标函数.


将打印详细信息的代码提炼为带两个参数的函数

二、如果局部变量是一个数据结构（例如数组、记录或者对象），而被提炼代码段又修改了这个结构中的数据。
可以将设置到期日的代码用同样的方式提炼出来
```
